# 构建时（服务端）请求 FastAPI 使用的地址
API_BASE_URL=http://localhost:8000

# 本地开发时在 .env.development 中设置，浏览器访问音频/封面使用该地址：
# NEXT_PUBLIC_MEDIA_BASE_URL=http://localhost:8000
# 生产静态导出时不要设置此变量，页面会使用 /media、/api 同域相对路径
